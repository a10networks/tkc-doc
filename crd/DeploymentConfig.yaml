---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: deploymentconfigs.tkc.a10networks.com
spec:
  group: tkc.a10networks.com
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                acosResourceConfigSummary:
                  type: array
                  x-kubernetes-validations:
                    - message: Commands cannot be updated. Please delete and create back.
                      rule: self == oldSelf
                  items:
                    type: string
                  description: ACOS cli commands for creating config on ACOS
                acosResourceConfigForDeletion:
                  type: array
                  x-kubernetes-validations:
                    - message: Commands cannot be updated. Please delete and create back.
                      rule: self == oldSelf
                  items:
                    type: string
                  description: ACOS cli commands for deleting config from ACOS
  scope: Namespaced
  names:
    plural: deploymentconfigs
    singular: deploymentconfig
    kind: DeploymentConfig
    shortNames:
      - a10dc
